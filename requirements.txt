# Discord library — handles the gateway connection and event callbacks
# (the subscribe/callback model similar to ROS topics).
# The [voice] extra pulls the voice dependencies (e.g. davey, PyNaCl) needed
# for the bot to join voice channels.
discord.py[voice]>=2.4.0

# OpenAI SDK — OpenRouter is OpenAI-compatible, so we use this client and
# just point its base_url at OpenRouter.
openai>=1.40.0

# Loads variables from a .env file into the environment so we never
# hard-code secrets in the source code.
python-dotenv>=1.0.1
